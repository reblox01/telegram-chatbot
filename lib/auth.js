const crypto=require('crypto');function base64urlDecode(str){let base64=str.replace(/-/g,'+').replace(/_/g,'/');const pad=4-(base64.length%4);if(pad!==4)base64+='='.repeat(pad);return Buffer.from(base64,'base64');}function verifyJWT(token,secret){const parts=token.split('.');if(parts.length!==3)return null;const[encodedHeader,encodedPayload,signature]=parts;const data=`${encodedHeader}.${encodedPayload}`;const expected=crypto.createHmac('sha256',secret).update(data).digest('base64').replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'');if(expected!==signature)return null;try{const payload=JSON.parse(base64urlDecode(encodedPayload).toString('utf8'));if(payload.exp&&Date.now()>=payload.exp*1000)return null;return payload;}catch(e){return null;}}module.exports={verifyJWT};