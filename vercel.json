{
  "version": 2,
  "builds": [
    { "src": "api/webhook.js", "use": "@vercel/node", "config": { "maxDuration": 60 } }
  ],
  "routes": [
    { "src": "/api/webhook", "dest": "/api/webhook.js" }
  ]
}
